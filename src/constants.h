
using namespace std;

// maximum acceleration allowed for the ego vehicle
const float MAX_ACCEL = 0.40;

// maximum speed allowed for the ego vehicle
const float MAX_SPEED = 49.0;

// distance threshold of front vehicle 
const double FRONT_THRESH = 35.0;